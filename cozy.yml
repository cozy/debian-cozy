host: @@ADDRESS@@
port: @@PORT@@

admin:
  host: @@ADMIN_ADDRESS@@
  port: @@ADMIN_PORT@@

couchdb:
  url: @@COUCHDB@@

fs:
  url: file:///var/lib/cozy

konnectors:
  cmd: /usr/share/cozy/konnector-nsjail-run.sh

log:
  level: info
  syslog: true
